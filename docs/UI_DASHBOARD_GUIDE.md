# Gradio Web UI Dashboard - Руководство пользователя

## 📋 Обзор

Gradio Web UI Dashboard - это центр управления для Domain-Specific MoE System, предоставляющий интерфейс для:
- Тестирования модели через чат
- Мониторинга системы в реальном времени
- Запуска тестов и просмотра coverage
- Обучения моделей с визуализацией прогресса
- Визуализации архитектуры AI системы

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Зависимости уже установлены:
# - gradio
# - plotly
# - networkx
# - psutil
# - pytest-cov
```

### Запуск Dashboard

```bash
# Простой запуск (порт 7860 по умолчанию)
python scripts/run_ui.py

# Запуск на custom порту
python scripts/run_ui.py --port 8080

# Запуск с публичной ссылкой (Gradio share)
python scripts/run_ui.py --share

# Запуск с custom конфигурацией
python scripts/run_ui.py --config my_config.yaml

# Debug режим
python scripts/run_ui.py --debug
```

После запуска откройте браузер по адресу: `http://localhost:7860`

## 🎯 Tabs и функциональность

### 1. 💬 Chat Interface

**Назначение:** Диалог с MoE системой через Router или конкретного эксперта

**Функциональность:**
- Автоматический выбор эксперта через Router
- Ручной выбор конкретного эксперта
- Параметры генерации (max tokens, temperature)
- История диалога
- Метрики последнего ответа:
  - Использованный эксперт
  - Routing confidence
  - Latency (ms)
  - Токенов сгенерировано
  - Статистика памяти

**Примеры запросов:**
```
"Напиши функцию на Python для сортировки списка"
"Реши уравнение: x² - 5x + 6 = 0"
"Объясни что такое машинное обучение"
```

### 2. 📊 Monitoring Panel

**Назначение:** Real-time мониторинг производительности и ресурсов

**Метрики:**
- CPU Usage (%)
- RAM Usage (MB)
- Loaded Experts (количество)
- Average Latency (ms)

**Графики:**
1. **Inference Latency** - время выполнения inference (ms)
2. **Throughput** - скорость генерации (tokens/sec)
3. **Memory Usage by Module** - использование памяти модулями (MB)
4. **Routing Statistics** - распределение запросов по экспертам

**Таблицы:**
- Active Experts (ID, Name, Status, Priority)
- Data Flow Metrics (From → To, Speed KB/s, Activity)

**Auto-refresh:** Обновление каждые 2 секунды (можно отключить)

### 3. 🏗️ Architecture Visualization

**Назначение:** Динамическая визуализация архитектуры AI системы

**Отображает:**
- **Узлы (модули):**
  - 🔴 Router - маршрутизация запросов
  - 🔵 Memory - трёхуровневая память
  - 🟢 Experts - domain-specific эксперты
  - 🟡 Trainer - обучение моделей

- **Размер узла** ~ количество параметров
- **Цвет узла** ~ тип модуля

- **Рёбра (связи):**
  - Толщина ~ скорость передачи данных (KB/s)
  - Прозрачность ~ уровень активности соединения

**Layouts:**
1. **Hierarchical** - иерархическая структура (Router → Experts → Output)
2. **Force-directed** - физическая симуляция (NetworkX spring layout)
3. **Circular** - круговое расположение

**Hover** на узлах/рёбрах показывает детали:
- Количество слоёв
- Параметров
- Использование памяти
- Скорость data flow

**Auto-refresh:** Обновление каждые 2 секунды для real-time data flow

### 4. 🧪 Test Panel

**Назначение:** Запуск pytest и просмотр результатов

**Функциональность:**
- Выбор набора тестов:
  - Все тесты (211 tests)
  - Transformer tests (24 tests)
  - Expert tests (31 tests)
  - Router tests (47 tests)
  - Memory tests (44 tests)
  - Dataset tests (39 tests)
  - Trainer tests (26 tests)

- Coverage анализ (опционально)
- Real-time прогресс выполнения

**Результаты:**
- Total/Passed/Failed/Skipped tests
- Duration (sec)
- Success Rate (%)
- Coverage по модулям (%)
- Детали провалившихся тестов

**Графики:**
- Coverage Report by Module (%)

### 5. 🎓 Training Panel

**Назначение:** Обучение моделей с real-time мониторингом

**⚠️ Статус:** В разработке (v0.4.0)

**Параметры обучения:**
- Expert ID
- Training Data (.txt или .jsonl)
- Epochs
- Batch Size
- Learning Rate
- Gradient Accumulation Steps
- Early Stopping Patience

**Real-time мониторинг:**
- Current Epoch
- Current Loss
- Current Perplexity
- Графики Loss/Perplexity curves

**Checkpoint Management:**
- Сохранение checkpoints
- Загрузка checkpoints
- Таблица сохранённых checkpoints

**Примечание:** Полная интеграция с Trainer планируется в версии 0.5.0

## ⚙️ Конфигурация

Конфигурация находится в `configs/ui_config.yaml`

```yaml
system:
  device: "cpu"                    # cpu или cuda
  max_loaded_experts: 3            # LRU cache размер
  metrics_history_size: 1000       # Circular buffer размер

ui:
  theme: "soft"                    # Gradio theme
  auto_refresh_interval_sec: 2.0   # Auto-refresh интервал

router:
  default_expert_id: "general"

memory:
  max_tokens_per_level: 250000     # 250k на уровень

experts:
  models_dir: "models/experts"
  available_experts:
    - id: "general"
      name: "General Assistant"
      priority: 3
      keywords: []

    - id: "python_expert"
      name: "Python Expert"
      priority: 8
      keywords: ["python", "код", "функция"]

monitoring:
  enable_system_metrics: true      # CPU/RAM мониторинг
  enable_dataflow_tracking: true   # Data flow трекинг
```

## 🏗️ Архитектура UI

```
src/python/ui/
├── app.py                       # Главное приложение
├── backend/
│   ├── moe_system.py           # Унифицированный интерфейс к MoE
│   ├── metrics_collector.py   # Сбор метрик
│   └── test_runner.py          # Интеграция с pytest
├── components/
│   ├── chat_interface.py       # Chat tab
│   ├── monitoring_panel.py     # Monitoring tab
│   ├── architecture_viz.py     # Architecture tab
│   ├── test_panel.py           # Tests tab
│   └── training_panel.py       # Training tab
└── utils/
    ├── graph_builder.py        # Построение графов
    └── plot_utils.py           # Утилиты для Plotly
```

## 🔧 Технологический стек

- **Gradio 6.2.0** - Web UI framework
- **Plotly 6.5.0** - Интерактивные графики и визуализация
- **NetworkX 3.6** - Графовые алгоритмы для layouts
- **psutil 7.2** - System metrics (CPU/RAM)
- **pytest-cov 7.0** - Coverage reporting

## 📊 Метрики производительности

### Backend
- **MetricsCollector**: Circular buffers (1000 точек)
- **Data Flow Tracking**: Последние 60 секунд
- **Memory Usage**: ~17 MB постоянно

### UI
- **Auto-refresh**: Каждые 2 секунды
- **Latency**: <50ms для обновления метрик
- **Графики**: До 50 точек истории на графике

## 🐛 Troubleshooting

### Проблема: UI не запускается

**Решение:**
```bash
# Проверьте установку зависимостей
pip list | grep gradio

# Переустановите если нужно
pip install --upgrade gradio plotly networkx psutil
```

### Проблема: Ошибка импорта модулей

**Решение:**
```bash
# Убедитесь что запускаете из корня проекта
cd /path/to/NM_LLM_Test_2
python scripts/run_ui.py
```

### Проблема: Port уже занят

**Решение:**
```bash
# Используйте другой порт
python scripts/run_ui.py --port 8080
```

### Проблема: Тесты не запускаются через UI

**Решение:**
```bash
# Проверьте что pytest установлен
pytest --version

# Запустите тесты напрямую для проверки
pytest tests/ -v
```

## 📝 Примеры использования

### Запуск тестов
1. Откройте tab "🧪 Tests"
2. Выберите "Все тесты" или конкретный модуль
3. Включите "Включить Coverage анализ"
4. Нажмите "▶️ Run Tests"
5. Дождитесь выполнения
6. Просмотрите результаты и coverage

### Мониторинг системы
1. Откройте tab "📊 Monitoring"
2. Включите "Auto-refresh (2s)"
3. Наблюдайте за метриками в реальном времени
4. Используйте "🔄 Refresh Now" для ручного обновления
5. "🗑️ Reset Metrics" для очистки истории

### Визуализация архитектуры
1. Откройте tab "🏗️ Architecture"
2. Выберите layout (Hierarchical / Force-directed / Circular)
3. Наблюдайте за data flow в реальном времени
4. Hover на узлах для просмотра деталей
5. Просмотрите Data Flow Metrics в таблице

## 🎯 Roadmap

### Версия 0.5.0 (планируется)
- ✅ Полная интеграция Training Panel с Trainer
- ✅ Real-time training progress
- ✅ Checkpoint management
- ✅ Визуализация learning curves

### Версия 0.6.0 (планируется)
- ✅ Learned Router интеграция
- ✅ Advanced Memory System визуализация
- ✅ Multi-expert inference
- ✅ Batch processing UI

## 📚 Дополнительные ресурсы

- **Architecture документация**: `docs/Plans/20260106_ARCHITECTURE.md`
- **API Reference**: `docs/Plans/20260106_API_REFERENCE.md`
- **Testing Report**: `docs/Reports/20260107_TESTING_REPORT.md`
- **Project Status**: `docs/Reports/20260107_PROJECT_STATUS.md`

---

**Версия документа:** 1.0
**Дата:** 2026-01-07
**Автор:** Claude Code
